#pragma once
#include <string>

//enum used to determine the degree program
enum DegreeProgram {SECURITY,NETWORK,SOFTWARE};

//gives back a string for each degree program 
static const std::string degreeStrings[] = { "SECURITY", "NETWORK", "SOFTWARE" };
